<tr>
  <td><%= good.name %></td>
  <td><%= good.price %></td>
  <td><%= good.utility %></td>
  <td>
    <%= link_to 'Edit', edit_question_good_path(good.question, good), :class => 'btn btn-mini btn-success' %>
    <%= link_to 'Destroy', [good.question, good], :method => :delete, :confirm => "Are you sure?", :class => 'btn btn-mini btn-danger'%>
  </td>
</tr>