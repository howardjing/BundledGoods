<tr>
  <td><%= bundle.name %></td>
  <td><%= bundle.goods_names %></td>
  <td><%= bundle.lambda %></td>
  <td><%= bundle.value %></td>
  <td><%= bundle.utility %></td>
  <td>
    <%= link_to 'Edit', edit_question_bundle_path(bundle.question, bundle), :class => 'btn btn-mini btn-success' %>
    <%= link_to 'Destroy', [bundle.question, bundle], :method => :delete, :confirm => "Are you sure?", :class => 'btn btn-mini btn-danger'%>
  </td>
</tr>