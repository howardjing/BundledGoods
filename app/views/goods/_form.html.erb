<%= form_for([@good.question, @good]) do |f| %>
    <% if @good.errors.any? %>
        <div class="alert alert-error">
          <h3><%= pluralize(@good.errors.count, "error") %> prohibited this question from being saved:</h3>
          <ul>
            <% @good.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :number %>
      <%= f.text_field :number %>
    </div>

    <div class="field">
      <%= f.label :price %>
      <%= f.text_field :price %>
    </div>

    <div class="field">
      <%= f.label :utility %>
      <%= f.text_field :utility %>
    </div>

    <div class="actions">
      <%= f.submit :class => 'btn btn-primary' %>
    </div>
<% end %>