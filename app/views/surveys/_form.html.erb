<%= form_for(@survey) do |f| %>
  <% if @survey.errors.any? %>
    <div class="alert alert-error">
      <h3><%= pluralize(@survey.errors.count, "error") %> prohibited this survey from being saved:</h3>
      <ul>
        <% @survey.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  
  <div class="field">
    <%= f.label :instructions %>
    <%= f.text_area :instructions, :class => "span10" %>
  </div>
  
  <div class="actions">
    <%= f.submit :class => 'btn btn-primary' %>
  </div>
<% end %>