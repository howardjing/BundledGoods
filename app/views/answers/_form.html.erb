<%= form_for([@answer.question.survey, @answer.question, @answer]) do |f| %>
  <% if @answer.errors.any? %>
    <div class="alert alert-error">
      <h3><%= pluralize(@answer.errors.count, "error") %> prohibited this answer from being saved:</h3>
      <ul>
        <% @answer.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content, :class => "span12" %>
  </div>
  
  <div class="actions">
    <%= f.submit :class => 'btn btn-primary' %>
  </div>
<% end %>