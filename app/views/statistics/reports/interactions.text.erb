userId, questionStatId, questionId, questionNumber, creationTime, content, 
<% QuestionStat.where('true = true').limit(10).each do |stat| %>
<%= stat.question.user_id %>, <%= stat.id %>, <%= stat.question_id %>, <%= stat.question.number %> <%= stat.created_at %>,  <%= stat.content %>,  
<% end %>
